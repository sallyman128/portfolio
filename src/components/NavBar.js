import { Navbar } from "react-bootstrap"
import {Container} from "react-bootstrap"

const NavBar = () => {
    return (
        <Navbar>
            <Container>
                
            </Container>
        </Navbar>
    )
}

export default NavBar